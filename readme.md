```> cd /path/to/demoqa_tests/folder```  
```> mvn clean test```

Учебная платформа demoqa.com не видимо не имеет адаптивной верстки, и поэтому у меня тест адает из-за ```Unable to find element```, потому что 1600x900.
